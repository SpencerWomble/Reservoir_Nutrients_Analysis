[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tnwater"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pandas", "tzdata"]

[tool.setuptools]
packages = ["tnwater"]dir